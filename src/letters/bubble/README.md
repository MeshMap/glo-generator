Drop 26 image files here, one per letter: `A.png`, `B.png`, … `Z.png`
(uppercase filenames, matching the letter exactly).

See `LETTER_IMAGES.md` in the project root for the full spec (size,
background, style-consistency tips). The app picks these up automatically —
no code changes needed, just refresh the page after adding files.
